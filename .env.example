# TEST_ENV=staging|production (optional; default staging)
# Copy to .env.staging / .env.production (or .env)
GENKI_TEST_EMAIL=your-email@example.com
GENKI_TEST_PASSWORD=your-password
GENKI_TEST_DISPLAY_NAME=Ashen
